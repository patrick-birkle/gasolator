spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/gasolator
    username: gasolator
    password: gasolator
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
